---
import Layout from "../layouts/Layout.astro";
---

<Layout title="The 1% Club | Nony">
  <!-- No Layout Wrapper, Full Screen Override -->
  <main class="editorial-wrapper">
    <div class="content-col">
      <header>
        <div class="eyebrow">PREMIUM ACCESS</div>
        <h1>The 1% Club</h1>
        <div class="price-tag">$2,500 / 60m</div>
      </header>

      <div class="body-text">
        <p class="lead">
          You could have bought a used Honda Civic. You chose to talk to a
          16-year-old developer. This is a questionable financial decision.
        </p>

        <p>
          But since you're here, you get the premier treatment. No hold music.
          No ticket queue. Just me, judging your codebase in real-time.
        </p>

        <ul class="offer-list">
          <li>
            <strong>The Roast</strong>
            <span
              >I will audit your project and explain why it's legacy code.</span
            >
          </li>
          <li>
            <strong>The Rant</strong>
            <span>Pick a topic (Next.js, Printers). I will dismantle it.</span>
          </li>
          <li>
            <strong>The Secret</strong>
            <span>Preview unreleased prototypes.</span>
          </li>
        </ul>

        <a
          href="mailto:nony@nony.cc?subject=Premium%20Slot%20Inquiry"
          class="cta-btn"
        >
          Reserve Slot
        </a>

        <p class="disclaimer">
          Recruiters will be hung up on. Printer support costs double.
        </p>
      </div>
    </div>

    <div class="visual-col">
      <div class="gold-block">
        <div class="sculpture-text">VIP</div>
      </div>
    </div>
  </main>
</Layout>

<style>
  :global(body) {
    background: #000 !important;
    color: #fff;
  }

  .editorial-wrapper {
    display: grid;
    grid-template-columns: 1fr 1fr;
    min-height: 100vh;
    width: 100%;
    position: fixed;
    inset: 0;
    z-index: 100;
    background: #000;
    overflow-y: auto;
  }

  .content-col {
    padding: 6rem;
    display: flex;
    flex-direction: column;
    justify-content: center;
    border-right: 1px solid rgba(255, 255, 255, 0.1);
  }

  .visual-col {
    display: flex;
    align-items: center;
    justify-content: center;
    background: radial-gradient(circle at center, #111, #000);
    position: relative;
    overflow: hidden;
  }

  .gold-block {
    width: 300px;
    height: 400px;
    background: linear-gradient(135deg, #cfc09f, #ffecb3, #b3a070, #52472b);
    border-radius: 4px;
    display: flex;
    align-items: center;
    justify-content: center;
    animation: float 6s ease-in-out infinite;
    box-shadow: 0 20px 50px rgba(180, 150, 50, 0.2);
  }

  .sculpture-text {
    font-family: "Fraunces", serif;
    font-size: 8rem;
    font-weight: 100;
    color: #000;
    opacity: 0.5;
    letter-spacing: -0.05em;
    mix-blend-mode: overlay;
  }

  @keyframes float {
    0%,
    100% {
      transform: translateY(0) rotate(0deg);
    }
    50% {
      transform: translateY(-20px) rotate(2deg);
    }
  }

  .eyebrow {
    font-family: "Fira Code", monospace;
    color: #b3a070;
    letter-spacing: 0.2em;
    margin-bottom: 2rem;
    font-size: 0.8rem;
  }

  h1 {
    font-family: "Fraunces", serif;
    font-size: 5rem;
    line-height: 0.9;
    margin-bottom: 1rem;
    letter-spacing: -0.03em;
  }

  .price-tag {
    font-family: "Fira Code", monospace;
    font-size: 1.5rem;
    color: #555;
    margin-bottom: 4rem;
  }

  .lead {
    font-size: 1.4rem;
    line-height: 1.4;
    margin-bottom: 2rem;
    color: #eee;
    font-family: "Fraunces", serif;
    font-style: italic;
  }

  p {
    color: #888;
    line-height: 1.6;
    margin-bottom: 2rem;
    max-width: 500px;
  }

  .offer-list {
    list-style: none;
    padding: 0;
    margin-bottom: 4rem;
    max-width: 500px;
  }
  .offer-list li {
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    padding: 1.5rem 0;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }
  .offer-list strong {
    font-family: "Fraunces", serif;
    font-size: 1.2rem;
    color: #fff;
  }
  .offer-list span {
    font-size: 0.9rem;
    color: #666;
  }

  .cta-btn {
    display: inline-block;
    border: 1px solid rgba(255, 255, 255, 0.2);
    color: #fff;
    padding: 1.2rem 3rem;
    text-decoration: none;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    font-size: 0.9rem;
    transition: all 0.3s;
  }
  .cta-btn:hover {
    background: #fff;
    color: #000;
  }

  .disclaimer {
    font-size: 0.75rem;
    margin-top: 2rem;
    color: #444;
  }

  @media (max-width: 900px) {
    .editorial-wrapper {
      grid-template-columns: 1fr;
      position: relative;
      overflow-y: visible;
    }
    .content-col {
      padding: 4rem 2rem;
      border-right: none;
    }
    .visual-col {
      height: 300px;
      order: -1;
    }
    .gold-block {
      height: 200px;
      width: 150px;
    }
    h1 {
      font-size: 3.5rem;
    }
  }
</style>
